{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_user_index'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_user_index'|trans }}</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <div class="button">
            <a href="{{ url('user_edit') }}" title="{{ 'user_edit'|trans }}">
                {{ 'action_edit'|trans }}
            </a>
        </div>
    {% endif %}
{% endblock %}