{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_book_comment_create'|trans({'%id%': book.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_book_comment_create'|trans({'%id%': book.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'POST', action: url('book_comment', {id: book.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('book_show', {id: book.id}) }}" class="btn btn-link">
            {{ 'action_back_to_book'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}