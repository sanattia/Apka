{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_trudnosc_details'|trans({'%id%': trudnosc.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_trudnosc_details'|trans({'%id%': trudnosc.id|default('')}) }}</h1>
    {% if trudnosc is defined and trudnosc|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'label_id'|trans }}</dt>
            <dd>{{ trudnosc.id }}</dd>
            <dt>{{ 'label_name'|trans }}</dt>
            <dd>{{  trudnosc.name }}</dd>
        </dl>

    {% else %}
        <p>
            {{ 'message_item_not_found'|trans }}
        </p>
    {% endif %}
    <p>
        <a href="{{ url('trudnosc_index') }}" title="{{ 'action_back_to_list'|trans }}">
            {{ 'action_back_to_list'|trans }}
        </a>
    </p>
{% endblock %}